---
# Restart only when notified by a config change (P1-5 / iac-basics §6:
# `state: restarted` is never idempotent, so it must be handler-triggered).
- name: Restart k3s
  ansible.builtin.systemd:
    name: k3s
    state: restarted
    daemon_reload: true
